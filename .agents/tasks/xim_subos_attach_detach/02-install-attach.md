# Task 02: Install Attach Flow

- keep `install` hook conditional on payload absence
- make `config` run for both fresh install and payload reuse
- keep explicit target activation scoped to requested targets only

