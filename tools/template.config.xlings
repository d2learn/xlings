xname = "ProjectName"
xdeps = {
    -- 不指定版本(一般默认最新版本)
    cpp = "",

    -- 精确版本
    python = "3.12",

    -- 版本范围
    vs = ">=2022",

    -- postprocess cmds
    xppcmds = {
        "echo hello xlings",
    }
}

xlings_name = "dslings"
xlings_lang = "cpp"
--xlings_editor = "vscode"
--xlings_err_skip = true
--xlings_llm_config = "llm.config.xlings" -- llm-config-file path