# Task 03: Home Bootstrap Init

## Goal

Implement shared home-layout initialization for both `self init` and
`self install`.

## Done When

- required `data/`, `subos/`, and `config/shell/` directories are generated
- builtin shims are created from `bin/xlings`
- `subos/current -> default` is created
